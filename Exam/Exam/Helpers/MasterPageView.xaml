<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Exam.Helpers.MasterPageView"
             Title="ImageList">

    <StackLayout>
        <Label Text="Enter Flickr search tags" />
        <Entry Placeholder="Enter tag here" x:Name="txtSearch"/>
        <Button Text="Search" x:Name="btnSearch" Clicked="BtnSearch_Clicked"/>
        <ListView x:Name="ListView" SelectedItem="ListView_ItemSelected" x:FieldModifier="public">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <ViewCell>
                        <Grid Padding="5">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="30"/>
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <Image Source="{Binding Icon}" />
                            <Label Grid.Column="1" Text="{Binding Title}" />
                        </Grid>
                    </ViewCell>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </StackLayout>
</ContentPage>